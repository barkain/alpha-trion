# === AI Provider Configuration ===
#
# The app auto-detects which API format to use:
#
#   1. Key starts with "sk-ant-"  -> Direct Anthropic API (x-api-key auth)
#   2. VITE_AI_BASE_URL contains "anthropic" -> Anthropic-format proxy (Bearer auth)
#      e.g. https://api.z.ai/api/anthropic  ->  detected as Anthropic proxy
#   3. Otherwise -> OpenAI-compatible (Bearer auth)
#
# For Anthropic proxies (case 2), the URL is used as-is with /v1/messages appended.

VITE_AI_API_KEY=your-api-key-here
VITE_AI_BASE_URL=
VITE_AI_MODEL=
